
#include <stdio.h>
#include "foo.h"

int coroutine quux (int baz, int bar)
{
    event e;
    coroutine foo f;
    endvars;

    call foo (&f, 1, 2, 3);
    getevent e;
    if (e == &f)
        break;
    return 8;
}

int test()
{
}



