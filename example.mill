imports:
    - foo

args:
    - int baz
    - int bar

vars:
    - int src
    - int event
    - struct foo_ctx f

body: |

    foo(&f);
    while(1) {
        getevent(&src, &event);
        if (event == 1)
            return;
    }

