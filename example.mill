
#include <stdio.h>

coroutine quux (int bar)
{
    coroutine wait w;
    event e;
    endvars;

    call wait (&w, 1000);
    getevent e;
    if (e == &w)
        printf ("timeout\n");
}

int main ()
{
    quux (1);
    return 0;
}




